
{{ form_start(form) }}
    {{ form_widget(form) }}
    <div  style="display: flex;align-items: center;justify-content:center;margin-top: 75px; padding: 100px;">
    
   <div class="col-sm-10">
  <a href="{{ path('app_reclamation_indexfront') }}"<button type="submit" class="btn btn-primary">Save</a>
                  </div>
 
{{ form_end(form) }}
